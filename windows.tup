# vim: set ts=8 sw=8 sts=8 noet tw=78 ft=tup:

ifeq (@(TUP_MSVC),y)
	include msvc.tup
else
	include gcc.tup
	CFLAGS += -D_NO_OLDNAMES
	CFLAGS += -I$(TUP_CWD)/src/compat/win32
endif

TUP_MONITOR = null

